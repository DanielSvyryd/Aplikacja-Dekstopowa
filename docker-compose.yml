services:
  mongodb:
    image: mongo:latest
    container_name: student-tracker-db
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: